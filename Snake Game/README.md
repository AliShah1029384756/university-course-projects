# Snake Game (Assembly)

## Overview

A low-level Snake game project written in x86 assembly (`snake.asm`).

## Build

```bash
nasm -o snake.com -fbin snake.asm
```

## Run

Run the generated COM file in a DOS-compatible environment/emulator.

## Attribution Note

The source header credits the original author information already present in `snake.asm`.
